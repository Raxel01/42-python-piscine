my requirements are :
   * create a package the way I want
   * it will appear on the list of installed packages . 'pip list'
   * 'pip show -v ft_package' will shows all package details
   * package will be installed usign one of the following methodes
        • pip install ./dist/ft_package-0.0.1.tar.gz
        • pip install ./dist/ft_package-0.0.1-py3-none-any.whl
   * impleent a count_in_list and return it !